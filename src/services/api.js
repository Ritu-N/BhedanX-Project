const request = async (path, options) => { const response = await fetch(path, options); if (!response.ok) throw new Error('Backend unavailable'); return response.json(); };
export const api = { probes: () => request('/api/probes'), alerts: () => request('/api/alerts'), system: () => request('/api/system'), history: (id) => request(`/api/probes/${id}/history`), telemetry: (data) => request('/api/telemetry', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify(data) }), acknowledge: (id) => request(`/api/alerts/${id}/acknowledge`, { method:'POST' }) };
