import express from 'express';
import cors from 'cors';
import Database from 'better-sqlite3';
import path from 'path';
import { fileURLToPath } from 'url';
const __dirname = path.dirname(fileURLToPath(import.meta.url));
const db = new Database(path.join(__dirname, 'bhedanx.sqlite'));
db.exec(`CREATE TABLE IF NOT EXISTS probes (id TEXT PRIMARY KEY, zone TEXT, sector TEXT, priorityScore INTEGER, status TEXT, signalQuality INTEGER, battery INTEGER, updatedAt INTEGER); CREATE TABLE IF NOT EXISTS telemetry (id INTEGER PRIMARY KEY AUTOINCREMENT, probeId TEXT, timestamp INTEGER, vibration INTEGER, acoustic INTEGER, persistence INTEGER, signalQuality INTEGER, priorityScore INTEGER, status TEXT); CREATE TABLE IF NOT EXISTS alerts (id INTEGER PRIMARY KEY AUTOINCREMENT, timestamp INTEGER, probeId TEXT, severity TEXT, message TEXT, score INTEGER, acknowledged INTEGER DEFAULT 0); CREATE TABLE IF NOT EXISTS missions (id TEXT PRIMARY KEY, startedAt INTEGER);`);
const app = express(); app.use(cors()); app.use(express.json());
const fallback = [{id:'P-01',zone:'Zone A',sector:'Sector 1',priorityScore:22,status:'NORMAL',signalQuality:92,battery:78},{id:'P-02',zone:'Zone A',sector:'Sector 2',priorityScore:45,status:'SUSPICIOUS',signalQuality:86,battery:76},{id:'P-03',zone:'Zone A',sector:'Sector 3',priorityScore:92,status:'CRITICAL',signalQuality:91,battery:78},{id:'P-04',zone:'Zone B',sector:'Sector 1',priorityScore:68,status:'HIGH_PRIORITY',signalQuality:88,battery:64}];
app.get('/api/probes', (_, res) => { const rows = db.prepare('SELECT * FROM probes').all(); res.json(rows.length ? rows : fallback); });
app.get('/api/probes/:id', (req,res) => res.json(db.prepare('SELECT * FROM probes WHERE id=?').get(req.params.id) || fallback.find(p=>p.id===req.params.id) || {error:'Probe not found'}));
app.get('/api/probes/:id/history', (req,res) => res.json(db.prepare('SELECT * FROM telemetry WHERE probeId=? ORDER BY timestamp DESC LIMIT 50').all(req.params.id).reverse()));
app.get('/api/alerts', (_,res) => res.json(db.prepare('SELECT * FROM alerts ORDER BY timestamp DESC LIMIT 50').all()));
app.get('/api/system', (_,res) => res.json({name:'BhedanX Rescue Command', gateway:'BX-LOCAL-01', status:'ONLINE', mode:'SIMULATED'}));
app.post('/api/telemetry', (req,res) => { const t=req.body; if (!t?.probeId || [t.vibration,t.acoustic,t.persistence,t.signalQuality].some(v=>typeof v!=='number')) return res.status(400).json({error:'Invalid telemetry'}); const score=t.priorityScore||0; const status=t.status||'NORMAL'; db.prepare('INSERT INTO telemetry (probeId,timestamp,vibration,acoustic,persistence,signalQuality,priorityScore,status) VALUES (?,?,?,?,?,?,?,?)').run(t.probeId,t.timestamp||Date.now(),t.vibration,t.acoustic,t.persistence,t.signalQuality,score,status); db.prepare('INSERT INTO probes (id,zone,sector,priorityScore,status,signalQuality,battery,updatedAt) VALUES (?,?,?,?,?,?,?,?) ON CONFLICT(id) DO UPDATE SET priorityScore=excluded.priorityScore,status=excluded.status,signalQuality=excluded.signalQuality,updatedAt=excluded.updatedAt').run(t.probeId,t.zone||'Unknown',t.sector||'Unknown',score,status,t.signalQuality,t.battery||0,Date.now()); if(score>=30) db.prepare('INSERT INTO alerts (timestamp,probeId,severity,message,score) VALUES (?,?,?,?,?)').run(Date.now(),t.probeId,status,score>=80?'Potential survivor activity detected':'Investigation recommended',score); res.json({ok:true}); });
app.post('/api/alerts/:id/acknowledge', (req,res) => { db.prepare('UPDATE alerts SET acknowledged=1 WHERE id=?').run(req.params.id); res.json({ok:true}); });
const dist = path.join(__dirname, '..', 'dist'); app.use(express.static(dist)); app.get('/*splat', (_,res)=>res.sendFile(path.join(dist,'index.html'))); app.listen(3001,()=>console.log('BhedanX API listening on http://localhost:3001'));
